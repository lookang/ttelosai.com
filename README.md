# Ttelos AI

The public website for [ttelosai.com](https://ttelosai.com/).

## Preview locally

Open `index.html` directly or run any static web server from the repository root.

## Deploy

The repository is ready for Cloudflare Pages. Use no build command and `/` as the output directory.
ttelosai.com
